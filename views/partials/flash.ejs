<% if (success && success.length) { %>
    <div class="alert alert-success alert-solid text-center" role="alert">
        <%= success %>
    </div>
<% } %> <% if (error && error.length) { %>
    <div class="alert alert-danger alert-solid text-center" role="alert">
        <%= error %>
    </div>
<% } %>
